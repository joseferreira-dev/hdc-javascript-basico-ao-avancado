let bus = {
	wheels: 8,
	passengersLimit: 40,
	doors: 2
}

console.log(`Number of wheels = ${bus.wheels}`);
console.log(`Max number of passengers = ${bus.passengers}`);
console.log(`Number of doors = ${bus.doors}`);