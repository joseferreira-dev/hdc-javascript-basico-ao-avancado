let names = ['Jose', 'Mary', 'Marcus', 'Emily', 'Paul'];

let myName = 'Jose';

if (names.includes(myName)) {
	console.log(`The name ${myName} is included in the array`);
} else {
	console.log(`The name ${myName} is not included in the array`);
}