let base1 = 2;
let base2 = 3;
let base3 = 18;
let exponent = 2;

console.log(`${base1} ^ ${exponent} = ${Math.pow(base1, exponent)}`);
console.log(`${base2} ^ ${exponent} = ${Math.pow(base2, exponent)}`);
console.log(`${base3} ^ ${exponent} = ${Math.pow(base3, exponent)}`);